# How to use Client_test.py

1. run `python -m venv testapps` at project root
1. Active venv(active.bat in "venvname"/Scripts), then `pip install requirements.txt`
1. cd front then run `python app.py`
1. Run the apps `python client_test.py`(This is CLI Application)
